# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

A Python project (currently a fresh PyCharm scaffold). The virtual environment is at `.venv/`.

## Commands

```bash
# Run the main script
python main.py
```

No build system, test runner, or linter is configured yet.
