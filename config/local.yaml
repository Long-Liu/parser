# Local development — values here are for local dev only
# In production/CI, override via: DB_PASSWORD, JWT_SECRET environment variables
debug: true

db:
  host: 127.0.0.1
  port: 3306
  user: root
  password: "1234"   # override with DB_PASSWORD env var in production
  database: excel_parser
  pool_size: 5

jwt:
  secret: "local-dev-secret-change-me"  # override with JWT_SECRET env var in production
  expiry_hours: 24
